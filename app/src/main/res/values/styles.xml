<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/red_500</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MyTabLayoutCustom" parent="TextAppearance.Design.Tab">
        <item name="android:textColor">@color/red_500</item>

        <item name="android:textSize">16sp</item>

    </style>

    <style name="Theme.Zhaw.TabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/purple_bg</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPaddingStart">6dp</item>
        <item name="tabPaddingEnd">6dp</item>
        <item name="tabBackground">@color/white</item>
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="tabTextAppearance">@style/TextAppearance.AppCompat.Small.Inverse</item>
        <item name="tabSelectedTextColor">@color/colorAccent</item>
        <item name="android:tabStripEnabled">true</item>


    </style>

</resources>
